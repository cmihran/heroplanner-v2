Review the conversation context and stage all changed files (excluding runtime artifacts like .db-shm, .db-wal, node_modules, etc.) and create a commit with a descriptive message summarizing what was done in this session.

Do NOT push to remote. Do NOT commit .env files or secrets.
